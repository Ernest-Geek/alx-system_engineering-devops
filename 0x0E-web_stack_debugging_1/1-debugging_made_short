#!/usr/bin/env bash
#Code make simple and short
sudo apt update
sudo apt-get install -y net-tools
sudo netstat -tulpn | grep -q :80 || { sudo nginx -t; sudo sed -i 's/\(listen\s*[^;]*\)\(;\)/\180\2/' /etc/nginx/nginx.conf; sudo systemctl restart nginx; sudo netstat -tulpn | grep :80; }
